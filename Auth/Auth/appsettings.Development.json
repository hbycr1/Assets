{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Non-dev connection strings will only be stored in AzureKeyVault
  "ConnectionStrings": {
    "AuthDbContext": "Host=localhost;Database=assets;Username=postgres;Password="
  },
  // Identity Clients
  "Clients": {
    "WebClient": {
      "Uri": "http://localhost:3000"
    },
  }
}
